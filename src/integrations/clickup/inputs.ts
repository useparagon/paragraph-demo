import { createInputs } from '@useparagon/types/clickup';

/**
 * define inputs here which can be used across workflows
 */
const integrationInputs = createInputs({
  list: {
    id: 'e4bf66f6-9d95-4352-b8f4-7cab2626778d',
    title: 'List',
    tooltip: '',
    required: false,
    type: 'list',
  },
});

export default integrationInputs;
